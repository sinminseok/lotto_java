is/has/can
위 3개 동사 네이밍은 boolean값을 리턴하는 용도로 많이쓰인다.
 - is 는 맞는지 틀린지를 판단하는 메서드 명에 쓰인다. ex) isNumber
 - has 는 데이터를 가지고 있는지 확인하는 메서드 명에 쓰인다. ex)hasData
 - can 은 할 수 있는지 없는지를 확인하는 메서드 명에 쓰인다. ex)canOrder

create,find,to,A-By-B
 - create 는 새로운 객체를 만든후 리턴해주는 메서드 명에 쓰인다. ex) createBoard()
 - find 는 데이터를 찾는 메서드 명에 많이쓰인다. ex)findBoard()
 - to는 해당 객체를 다른 형태의 객체로 변환해주는 메서드 명으로 쓰인다. ex)toString
 - A-By-B는 B를 기준으로 A를 하겠다는 메서드 명으로 쓰인다. ex)getUserByName

